stages: 
  - build
  - deploy

build_job: 
  stage: build
  image: node:22
  script: 
    - npm ci
    - npm run build -- --mode production 
  artifacts: 
    paths:
      - dist/
  only:
    - main
  tags:
    - deploy-runner-demo

deploy_job:
  stage: deploy
  image: alpine:latest
  before_script:
    - apk add --no-cache lftp
  script:
    - lftp -c "
        open -u $FTP_USER,$FTP_PASSWORD sftp://ikamx.dreamhost.com;
        mirror -R --delete --verbose dist /home/ikamx/ika.mx/;
      "
  only: 
    - main
  tags:
    - deploy-runner-demo
